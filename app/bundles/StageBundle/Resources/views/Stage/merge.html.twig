{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.stage_to_merge) }}
    </div>
    <div class="form-group">
        {{ form_row(form.buttons) }}
    </div>
{{ form_end(form) }}
