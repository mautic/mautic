{{ include('MauticFormBundle:Field:group.html.twig', {
        'field': field,
        'fields': fields|default([]),
        'inForm': inForm|default(false),
        'id': id,
        'formId': formId|default(0),
        'formName': formName|default(''),
        'type': 'radio',
        'contactFields': contactFields|default([]),
        'companyFields': companyFields|default([]),
}) }}
