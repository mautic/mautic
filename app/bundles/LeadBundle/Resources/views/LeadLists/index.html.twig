{#
  Variables
    - lists
    - leadsLists
    - lead
#}
<ul class="list-group">
    {% for l in lists %}
        <li class="list-group-item">
            <i class="fa fa-lg fa-fw {% if leadLists[l.id] is defined %}fa-toggle-on text-success{% else %}fa-toggle-off text-danger{% endif %}"
               id="leadListToggle{{ l.id }}"
               onclick="Mautic.toggleLeadList('leadListToggle{{ l.id }}', {{ lead.id }}, {{ l.id }});"></i>
            <span>{{ l.name }} ({{ l.alias }})</span>
        </li>
    {% endfor %}
</ul>
