{#
  Variables
    - items
    - columns
    - security
    - currentList
    - permissions
    - noContactList
#}
{% for item in items %}
  {{ include('MauticLeadBundle:Lead:_list_row.html.twig') }}
{% endfor %}
