language: php

php:
  # At the moment Composer can't resolve dependencies for these PHP versions.
  #- 5.3
  #- 5.4
  #- 5.5
  - 5.6
  - 7.0
  - 7.1

before_script:
  # Remove composer.lock on php < 7.0
  - if [ "${TRAVIS_PHP_VERSION//./}" -eq "56" ]; then rm composer.lock; fi;
  - composer install

script:
  - composer run-script test
