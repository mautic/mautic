name: 'Split up the monorepo into subrepositories for composer purposes'

on:
    push:
        # Only trigger for specific branches or changes in specific paths.
        branches:
            - '[0-9].*'
            - 'gitsplit-action-debug'
        paths:
            - app/**
            - plugins/**
            - themes/**
            # also run when actions are being modified for testing purposes.
            - .github/** 
        # Tag push events should be ignored, they will be handled with the create event below.
        tags-ignore:
            - '*'
    create:
        tags:
            - '*'
        branches:
            - '[0-9].*'
            - 'gitsplit-action-debug'
    delete:
        tags:
            - '*'
        branches:
            - '[0-9].*'
            - 'gitsplit-action-debug'

jobs:
    sync_core_lib_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from app folder to mautic/core-lib
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0

            # Add a personal access token to the repository secrets. This will allow the splitter action to push the new commits
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'

            # Cache the splitsh executable to speedup future runs
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-core-lib-splitsh-v101'

            # Sync commits and tags for the configured subtree splits
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/core-lib.json # Reference the location where you saved your config file

    sync_plugin_grapesjs_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from grapesjs plugin to mautic/plugin-grapesjs-builder
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-grapesjs-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-grapesjs.json

    sync_plugin_citrix_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from citrix plugin to mautic/plugin-citrix
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-citrix-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-citrix.json

    sync_plugin_clearbit_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from clearbit plugin to mautic/plugin-clearbit
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-clearbit-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-clearbit.json

    sync_plugin_cloudstorage_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from cloudstorage plugin to mautic/plugin-cloudstorage
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-cloudstorage-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-cloudstorage.json

    sync_plugin_crm_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from crm plugin to mautic/plugin-crm
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-crm-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-crm.json

    sync_plugin_emailmarketing_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from emailmarketing plugin to mautic/plugin-emailmarketing
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-emailmarketing-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-emailmarketing.json

    sync_plugin_focus_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from focus plugin to mautic/plugin-focus
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-focus-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-focus.json

    sync_plugin_fullcontact_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from fullcontact plugin to mautic/plugin-fullcontact
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-fullcontact-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-fullcontact.json

    sync_plugin_gmail_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from gmail plugin to mautic/plugin-gmail
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-gmail-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-gmail.json

    sync_plugin_outlook_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from outlook plugin to mautic/plugin-outlook
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-outlook-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-outlook.json
    
    sync_plugin_social_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from focus plugin to mautic/plugin-social
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-social-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-social.json

    sync_plugin_tagmanager_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from tagmanager plugin to mautic/plugin-tagmanager
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-tagmanager-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-tagmanager.json

    sync_plugin_zapier_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from zapier plugin to mautic/zapier-focus
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-plugin-focus-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/plugin-zapier.json

    sync_theme_aurora_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme aurora to mautic/theme-aurora
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-aurora-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-aurora.json
        
    sync_theme_blank_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme blank to mautic/theme-blank
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-blank-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-blank.json

    sync_theme_brienz_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme brienz to mautic/theme-brienz
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-brienz-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-brienz.json

    sync_theme_cards_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme cards to mautic/theme-cards
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-cards-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-cards.json

    sync_theme_coffee_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme coffee to mautic/theme-coffee
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-coffee-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-coffee.json
        
    sync_theme_confirmme_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme confirmme to mautic/theme-confirmme
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-confirmme-splitsh-v101'
            -   name: subtree 
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-confirmme.json
        
    sync_theme_fresh_center_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme fresh-center to mautic/theme-fresh-center
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-fresh-center-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-fresh-center.json
        
    sync_theme_fresh_fixed_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme fresh-fixed to mautic/theme-fresh-fixed
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-fresh-fixed-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-fresh-fixed.json

    sync_theme_fresh_left_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme fresh-left to mautic/theme-fresh-left
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-fresh-left-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-fresh-left.json

    sync_theme_fresh_wide_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme fresh-wide to mautic/theme-fresh-wide
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-fresh-wide-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-fresh-wide.json
        
    sync_theme_goldstar_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme goldstar to mautic/theme-goldstar
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-goldstar-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-goldstar.json

    sync_theme_mauve_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme mauve to mautic/theme-mauve
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-mauve-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-mauve.json

    sync_theme_nature_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme nature to mautic/theme-nature
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-nature-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-nature.json

    sync_theme_neopolitan_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme neopolitan to mautic/theme-neopolitan
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-neopolitan-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-neopolitan.json
        
    sync_theme_oxygen_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme oxygen to mautic/theme-oxygen
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-oxygen-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-oxygen.json

    sync_theme_paprika_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme paprika to mautic/theme-paprika
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-paprika-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-paprika.json
        
    sync_theme_skyline_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme skyline to mautic/theme-skyline
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-skyline-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-skyline.json

    sync_theme_sunday_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme sunday to mautic/theme-sunday
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-sunday-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-sunday.json
        
    sync_theme_trulypersonal_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme trulypersonal to mautic/theme-trulypersonal
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-trulypersonal-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-trulypersonal.json
        
    sync_theme_vibrant_commits:
        if: github.repository_owner == 'mautic'
        runs-on: ubuntu-latest
        name: Sync commits from theme vibrant to mautic/theme-vibrant
        steps:
            -   uses: actions/checkout@v2
                with:
                    persist-credentials: false
                    fetch-depth: 0
            -   uses: frankdejonge/use-github-token@1.0.1
                with:
                    authentication: 'username:${{ secrets.ACCESS_TOKEN }}'
                    user_name: 'Mautic CI'
                    user_email: 'ci@mautic.org'
            -   name: Cache splitsh-lite
                uses: actions/cache@v2
                with:
                    path: './splitsh'
                    key: '${{ runner.os }}-theme-vibrant-splitsh-v101'
            -   name: subtree split
                uses: acrobat/subtree-splitter@v1.0.0
                with:
                    config-path: .github/workflows/gitsplit/theme-vibrant.json
        