# Contact field quick filters

The Contact fields list now provides quick filters for `Indexed` and `Unique`.

Before this change, reviewing indexed or unique fields required opening the search controls, entering `is:indexed` or `is:unique`, and applying the search. The Quick filters menu now applies either existing search command directly, while the full search remains available for more complex queries.
