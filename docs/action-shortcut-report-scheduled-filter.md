# Scheduled reports quick filter

The Reports list now offers a visible `Scheduled reports` quick filter. It
reuses the existing `is:scheduled` search command and only changes the list
query; it does not edit reports or send email.

Before this shortcut, a user had to open Quick filters, remember and type
`is:scheduled`, and apply the search. The visible filter reduces that repeated
decision from three actions to one.
